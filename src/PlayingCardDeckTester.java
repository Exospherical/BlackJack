public class PlayingCardDeckTester {
    public static void main(String[] args) {
        //PlayingCardDeck playingcarddeck = new PlayingCardDeck();
        //PlayingCardDeck playingcarddeck2 = new PlayingCardDeck();

        //playingcarddeck.getDeckContents();
        //PlayingCard playingcard = new PlayingCard(PlayingCard.SPADES, PlayingCard.QUEEN);
        //new PrintAsciiCard(playingcard).printCard();
        //System.out.println(playingcarddeck.getnewDeck().get(0).getValue());
        //new PrintAsciiCard(playingcard).printCard();
        //playingcarddeck.shuffleDeck();
        //playingcarddeck.reorderDeck();

        //playingcarddeck.getDeckContents();
        //System.out.println(playingcarddeck.getnewDeck().size());
        //System.out.println(playingcarddeck.size());
        //playingcarddeck.shuffleDeck();
        //playingcarddeck.drawCards();
        // System.out.println(playingcarddeck.getnewDeck().size());
        //playingcarddeck.getDeckContents();
        //PlayingCard[] handofcards = playingcarddeck.drawCards();
        // playingcarddeck.reorderDeck();
        //playingcarddeck.getDeckContents();
        // for (int i = 0; handofcards.length > i; i++) {
        //   new PrintAsciiCard(handofcards[i]).printCard();
        // }

    }
}
